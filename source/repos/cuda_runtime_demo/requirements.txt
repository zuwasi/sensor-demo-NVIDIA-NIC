# CUDA Profiler GUI - Runtime Dependencies
# Install with: pip install -r requirements.txt

matplotlib>=3.5.0
numpy>=1.21.0

# Note: tkinter is part of Python's standard library and doesn't need to be installed separately
